import { TablaEditableDirective } from './tabla-editable.directive';

describe('TablaEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new TablaEditableDirective();
    expect(directive).toBeTruthy();
  });
});
